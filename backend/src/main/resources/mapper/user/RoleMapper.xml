<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.storyworkshop.module.user.mapper.RoleMapper">

    <resultMap id="RoleResultMap" type="com.example.storyworkshop.module.user.entity.Role">
        <id property="roleId" column="role_id"/>
        <result property="roleName" column="role_name"/>
        <result property="description" column="description"/>
    </resultMap>

    <sql id="BaseColumnList">
        role_id,
        role_name,
        description
    </sql>

    <select id="selectById" resultMap="RoleResultMap">
        SELECT
        <include refid="BaseColumnList"/>
        FROM role
        WHERE role_id = #{roleId}
    </select>

    <select id="selectByName" resultMap="RoleResultMap">
        SELECT
        <include refid="BaseColumnList"/>
        FROM role
        WHERE role_name = #{roleName}
        LIMIT 1
    </select>

    <select id="selectAll" resultMap="RoleResultMap">
        SELECT
        <include refid="BaseColumnList"/>
        FROM role
        ORDER BY role_id ASC
    </select>

    <select id="selectByUserId" resultMap="RoleResultMap">
        SELECT
            r.role_id,
            r.role_name,
            r.description
        FROM role r
        INNER JOIN user_role ur ON r.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
        ORDER BY r.role_id ASC
    </select>

    <select id="selectRoleNamesByUserId" resultType="string">
        SELECT r.role_name
        FROM role r
        INNER JOIN user_role ur ON r.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
        ORDER BY r.role_id ASC
    </select>

</mapper>