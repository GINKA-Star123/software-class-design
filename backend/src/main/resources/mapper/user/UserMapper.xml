<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.storyworkshop.module.user.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.example.storyworkshop.module.user.entity.User">
        <id property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="email" column="email"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumnList">
        user_id,
        username,
        password,
        nickname,
        email,
        avatar_url,
        status,
        create_time,
        update_time
    </sql>

    <select id="selectById" resultMap="UserResultMap">
        SELECT
        <include refid="BaseColumnList"/>
        FROM `user`
        WHERE user_id = #{userId}
    </select>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT
        <include refid="BaseColumnList"/>
        FROM `user`
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="selectByEmail" resultMap="UserResultMap">
        SELECT
        <include refid="BaseColumnList"/>
        FROM `user`
        WHERE email = #{email}
        LIMIT 1
    </select>

    <select id="countByUsername" resultType="int">
        SELECT COUNT(1)
        FROM `user`
        WHERE username = #{username}
    </select>

    <select id="countByEmail" resultType="int">
        SELECT COUNT(1)
        FROM `user`
        WHERE email = #{email}
    </select>

    <insert id="insert"
            parameterType="com.example.storyworkshop.module.user.entity.User"
            useGeneratedKeys="true"
            keyProperty="userId">
        INSERT INTO `user` (
            username,
            password,
            nickname,
            email,
            avatar_url,
            status
        )
        VALUES (
            #{username},
            #{password},
            #{nickname},
            #{email},
            #{avatarUrl},
            #{status}
        )
    </insert>

    <update id="updateProfile" parameterType="com.example.storyworkshop.module.user.entity.User">
        UPDATE `user`
        SET
            nickname = #{nickname},
            email = #{email},
            avatar_url = #{avatarUrl},
            update_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <update id="updateStatus">
        UPDATE `user`
        SET
            status = #{status},
            update_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <select id="selectAdminPage" resultMap="UserResultMap">
        SELECT
        <include refid="BaseColumnList"/>
        FROM `user`
        <where>
            <if test="keyword != null and keyword != ''">
                AND (
                    username LIKE CONCAT('%', #{keyword}, '%')
                    OR nickname LIKE CONCAT('%', #{keyword}, '%')
                    OR email LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countAdminPage" resultType="long">
        SELECT COUNT(1)
        FROM `user`
        <where>
            <if test="keyword != null and keyword != ''">
                AND (
                    username LIKE CONCAT('%', #{keyword}, '%')
                    OR nickname LIKE CONCAT('%', #{keyword}, '%')
                    OR email LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

</mapper>